# Netlify Configuration File
# This ensures that React Router (BrowserRouter) works correctly on page refresh/direct URL access.

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
